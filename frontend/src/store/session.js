const SESSIONADD = "session/add"
const SESSIONREMOVE = "session/remove"


const sessionAdd = () => {{
    type: SESSIONADD
}}

const sessionRemove = () => {{
    type: SESSIONREMOVE
}}

const sessionReducer = (state=initialState, action) => {
    let newState = {};
    // if()
    switch(action.type){

    }
}
